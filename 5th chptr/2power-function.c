#include<stdio.h>
#include<math.h>        //this library is compalsary to use power function.

int main(){
float x;
printf("enter ur number: ");
scanf("%f", &x);
printf("%f", pow(x,2));         // power = pow(number, power).

return 0;
}